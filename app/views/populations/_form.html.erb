<%= form_for @population, :html => { :class => 'form-horizontal' } do |f| %>
<%= render :partial => "layouts/messages", :locals => {:object => @population} %>  
  <div class="control-group">
    <%= f.label :target, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :target, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                populations_path, :class => 'btn' %>
  </div>
<% end %>
